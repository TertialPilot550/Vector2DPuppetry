package com.sammc.puppet_application.Entity;

import java.util.List;
/**
 * Animations will be represented as a list of the entity, where each entity should be the same, just modified from frame to frame.
 * The first element in the list is frame 0.
 * @author sammc
 */
public class Animation {
    public List<Entity> animation;
}
